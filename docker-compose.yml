services:
  mcdr:
    build: .
    ports:
      - "25565:25565"
      - "8000:8000"
    container_name: mcdr
    volumes:
      - ./data:/data
    command: start_mcdr.sh
    restart: unless-stopped
    tty: true
    stdin_open: true
