services:
  mcdr:
    build: .
    ports:
      - "25565-25665:25565"
      - "8000-8100:8000"
    volumes:
      - ./data:/data
    command: start_mcdr.sh
    restart: unless-stopped
    tty: true
    stdin_open: true
